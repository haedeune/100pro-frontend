# 백엔드 API 서버 URL
VITE_API_BASE_URL=http://localhost:8000

# Kakao JavaScript key (client-side key)
# Kakao Developers > 앱 > 앱 키 > JavaScript 키
VITE_KAKAO_JS_KEY=

# Optional: defaults to `${window.location.origin}/auth/kakao/callback` when empty
VITE_KAKAO_REDIRECT_URI=
